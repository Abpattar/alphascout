# Core
pyyaml>=6.0
python-dotenv>=1.0
cryptography>=41.0

# HTTP & Scraping
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
feedparser>=6.0
aiohttp>=3.9

# Data
pandas>=2.1
yfinance>=0.2

# AI Providers (OpenRouter/Gemini/Cerebras/NIM use plain requests; only Groq needs its SDK)
groq>=0.4

# Scheduling
apscheduler>=3.10

# Testing
pytest>=7.4
pytest-asyncio>=0.21

# ── Optional (setup wizard Zerodha integration; not needed to run the bot) ──
# kiteconnect>=4.0
